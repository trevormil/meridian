import { Landing07 } from '@/components/landings/Landing07';

/**
 * Home — pyramid lifecycle landing.
 * Brand-forward hero: Meridian glyph + wordmark, glowing live trading-day
 * timeline (8 AM Create → 9:30 Open → 4:00 Close → 4:05 Settle), and the
 * day's active markets below the fold.
 *
 * The full browse experience (tabs by status, group-by-ticker, etc) lives
 * at `/markets` — see app/markets/page.tsx.
 */
export default function Home() {
  return <Landing07 />;
}
